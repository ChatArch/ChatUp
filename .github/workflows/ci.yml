name: CI

on:
  push:
    branches:
      - main
      - master
  pull_request:

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.10", "3.11", "3.12"]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
      - run: python -m pip install --upgrade pip
      - run: python -m pip install -e ".[dev,docs]"
      - run: python -m pytest -q
      - name: Verify installed CLI contract
        run: |
          chatup --version
          chatup --tree
          chatup --tree-brief
      - run: python -m build
      - run: python -m twine check dist/*
      - name: Verify built wheel CLI contract
        run: |
          python -m venv "$RUNNER_TEMP/chatup-wheel"
          "$RUNNER_TEMP/chatup-wheel/bin/python" -m pip install dist/*.whl
          (
            cd "$RUNNER_TEMP"
            "$RUNNER_TEMP/chatup-wheel/bin/chatup" --version
            "$RUNNER_TEMP/chatup-wheel/bin/chatup" --tree
            "$RUNNER_TEMP/chatup-wheel/bin/chatup" --tree-brief
          )
      - run: mkdocs build --strict
